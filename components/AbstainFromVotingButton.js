import React from 'react';
import {Text, View, TouchableOpacity, StyleSheet} from 'react-native';

const AbstainFromVotingButton = () => {
    return (
        <TouchableOpacity>
            <View>
                <Text>
                    Abstain Vote
                </Text>
            </View>
        </TouchableOpacity>
    )
};

const styles = StyleSheet.create({});

export default AbstainFromVotingButton;